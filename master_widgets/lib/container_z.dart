import 'package:flutter/material.dart';

class ContainerZ extends StatelessWidget {
  const ContainerZ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      height: 100,
      color: Colors.blue,
    );
  }
}
